---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: portforwards.router.willful.be
spec:
  group: router.willful.be
  names:
    kind: PortForward
    listKind: PortForwardList
    plural: portforwards
    singular: portforward
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PortForward is the Schema for the portforwards API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PortForwardSpec defines the desired state of PortForward
            properties:
              for:
                properties:
                  service:
                    properties:
                      name:
                        type: string
                      number:
                        properties:
                          name:
                            type: integer
                        required:
                        - name
                        type: object
                    required:
                    - name
                    - number
                    type: object
                required:
                - service
                type: object
              rule:
                properties:
                  externalPortEnd:
                    description: 'External port end - IF NOT SET: The value will be
                      the same as external port start'
                    type: integer
                  externalPortStart:
                    description: External port start
                    type: integer
                  interface:
                    description: Interface
                    type: string
                  internalPortEnd:
                    description: 'Internal port end - IF NOT SET: The value will be
                      the same as internal port start'
                    type: integer
                  internalPortStart:
                    description: Internal port start
                    type: integer
                  protocol:
                    description: Protocol <TCP/UDP|TCP|UDP>
                    enum:
                    - TCP
                    - UDP
                    - TCP/UDP
                    type: string
                  serverIP:
                    description: Server IP address
                    type: string
                  serviceName:
                    description: Service name
                    type: string
                required:
                - externalPortEnd
                - externalPortStart
                - interface
                - internalPortEnd
                - internalPortStart
                - protocol
                type: object
            required:
            - rule
            type: object
          status:
            description: PortForwardStatus defines the observed state of PortForward
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
