apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-ingestion.fullname" . }}
  labels:
    {{- include "service-ingestion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-ingestion.selectorLabels" . | nindent 4 }}
